apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.name }}-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.name }}-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.name }}-db
  template:
    metadata:
      labels:
        app: {{ .Values.name }}-db
    spec:
      volumes:
        - name: {{ .Values.name }}-db
          persistentVolumeClaim:
            claimName: {{ .Values.name }}-db
      containers:
        - name: {{ .Values.name }}-db
          image: mongo:4
          ports:
            - containerPort: 27017
          volumeMounts:
            - mountPath: /data/db
              name: {{ .Values.name }}-db
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-db
spec:
  ports:
    - port: 27017
  selector:
    app: {{ .Values.name }}-db
  type: ClusterIP
