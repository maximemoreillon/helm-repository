apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "face-recognition-fastapi.fullname" . }}-test-connection"
  labels:
{{ include "face-recognition-fastapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "face-recognition-fastapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
