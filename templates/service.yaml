apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-recognition-fastapi.fullname" . }}
  labels:
    {{- include "face-recognition-fastapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "face-recognition-fastapi.selectorLabels" . | nindent 4 }}
