{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mosquitto-user-manager-mongoose.serviceAccountName" . }}
  labels:
{{ include "mosquitto-user-manager-mongoose.labels" . | nindent 4 }}
{{- end -}}
