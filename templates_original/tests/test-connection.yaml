apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mosquitto-user-manager-mongoose.fullname" . }}-test-connection"
  labels:
{{ include "mosquitto-user-manager-mongoose.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mosquitto-user-manager-mongoose.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
