apiVersion: v1
kind: Service
metadata:
  name: {{ include "mosquitto-user-manager-mongoose.fullname" . }}
  labels:
    {{- include "mosquitto-user-manager-mongoose.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mosquitto-user-manager-mongoose.selectorLabels" . | nindent 4 }}
