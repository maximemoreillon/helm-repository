apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-manager-mongoose.fullname" . }}
  labels:
    {{- include "user-manager-mongoose.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-manager-mongoose.selectorLabels" . | nindent 4 }}
