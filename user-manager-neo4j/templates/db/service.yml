apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-neo4j
spec:
  ports:
  - port: 7474
    name: http
  - port: 7687
    name: bolt
  selector:
    app: {{ .Values.name }}-neo4j
  # TODO: consider NodePort
  type: ClusterIP
