apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-api
spec:
  type: NodePort
  selector:
    app: {{ .Values.name }}-api
  ports:
  - port: 80
    nodePort: {{ .Values.nodePorts.api }}
