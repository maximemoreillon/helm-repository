apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-manager-neo4j.fullname" . }}
  labels:
    {{- include "user-manager-neo4j.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-manager-neo4j.selectorLabels" . | nindent 4 }}
