apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-manager-neo4j.fullname" . }}-test-connection"
  labels:
{{ include "user-manager-neo4j.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "user-manager-neo4j.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
