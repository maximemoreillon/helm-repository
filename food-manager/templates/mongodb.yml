apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "resources.prefix" . }}-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "resources.prefix" . }}-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "resources.prefix" . }}-db
  template:
    metadata:
      labels:
        app: {{ include "resources.prefix" . }}-db
    spec:
      volumes:
        - name: {{ include "resources.prefix" . }}-db
          persistentVolumeClaim:
            claimName: {{ include "resources.prefix" . }}-db
      containers:
        - name: {{ include "resources.prefix" . }}-db
          image: mongo:4
          ports:
            - containerPort: 27017
          volumeMounts:
            - mountPath: /data/db
              name: {{ include "resources.prefix" . }}-db
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "resources.prefix" . }}-db
spec:
  ports:
    - port: 27017
  selector:
    app: {{ include "resources.prefix" . }}-db
  type: ClusterIP
