apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "food-manager.prefix" . }}-gui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "food-manager.prefix" . }}-gui
  template:
    metadata:
      labels:
        app: {{ include "food-manager.prefix" . }}-gui
    spec:
      containers:
      - name: {{ include "food-manager.prefix" . }}-gui
        image: moreillon/food-manager-front
        ports:
        - containerPort: 80
        env:
        - name: VUE_APP_FOOD_MANAGER_API_URL
          value: /proxy/api
        - name: VUE_APP_LOGIN_URL
          value: /proxy/user-manager/auth/login
        - name: VUE_APP_IDENTIFICATION_URL
          value: /proxy/user-manager/users/self
        - name: PROXY_API
          value: http://{{ include "food-manager.prefix" . }}
        - name: PROXY_USER_MANAGER
          value: http://{{ include "food-manager.prefix" . }}-{{ toYaml (index .Values "user-manager-mongoose" "prefix" ) }}-api
        
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "food-manager.prefix" . }}-gui
spec:
  type: NodePort
  selector:
    app: {{ include "food-manager.prefix" . }}-gui
  ports:
  - port: 80
    nodePort: {{.Values.nodePort}}
