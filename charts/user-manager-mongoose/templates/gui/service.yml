apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-manager.prefix" . }}-gui
spec:
  type: ClusterIP
  selector:
    app: {{ include "user-manager.prefix" . }}-gui
  ports:
  - port: 80
