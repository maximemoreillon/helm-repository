apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-manager.fullname" . }}-gui
spec:
  type: ClusterIP
  selector:
    app: {{ include "user-manager.fullname" . }}-gui
  ports:
  - port: 80
