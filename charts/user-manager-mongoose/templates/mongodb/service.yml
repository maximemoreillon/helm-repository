{{- if .Values.mongodb.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-manager.prefix" . }}-mongo
spec:
  ports:
  - port: 27017
  selector:
    app: {{ include "user-manager.prefix" . }}-mongo
  type: ClusterIP
  
{{- end }}
