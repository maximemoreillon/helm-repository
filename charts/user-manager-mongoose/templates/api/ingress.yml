{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "user-manager.fullname" . }}-api
  annotations:
    {{- with .Values.ingress.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  tls:
    - hosts:
        - api.{{ include "user-manager.ingress.host" . }}
      secretName: {{ include "user-manager.fullname" . }}-api
  rules:
    - host: api.{{ include "user-manager.ingress.host" . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: 
                name: {{ include "user-manager.fullname" . }}-api
                port:
                  number: 80
{{- end }}
