apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "user-manager.fullname" . }}-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "user-manager.fullname" . }}-api
  template:
    metadata:
      labels:
        app: {{ include "user-manager.fullname" . }}-api
    spec:
      containers:
      - name: {{ include "user-manager.fullname" . }}-api
        # TODO: tag
        image: moreillon/user-manager-mongoose
        ports:
        - containerPort: 80
        env:
        - name: MONGODB_URL
          value: {{ include "user-manager.mongodb.url" . }} 
        - name: JWT_SECRET
          value: {{ .Values.jwt_secret }}
