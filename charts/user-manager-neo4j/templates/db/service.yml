apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-manager.prefix" . }}-neo4j
spec:
  ports:
  - port: 7474
    name: http
  - port: 7687
    name: bolt
  selector:
    app: {{ include "user-manager.prefix" . }}-neo4j
  # TODO: consider NodePort
  type: ClusterIP
