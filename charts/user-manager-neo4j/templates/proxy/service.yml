apiVersion: v1
kind: Service
metadata:
  name: {{include "user-manager.prefix" .}}-proxy
spec:
  type: NodePort
  selector:
    app: {{include "user-manager.prefix" .}}-proxy
  ports:
  - port: 80
    nodePort: {{ .Values.nodePorts.gui }}
