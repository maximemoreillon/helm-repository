# Default values for user-manager-neo4j.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# TODO: allow users to not deploy Neo4J
neo4j:
  username: neo4j
  password: neo4jpassword
  pvcSize: 100Gi

admin:
  username: admin
  password: admin

nodePorts:
  api: 30016
  gui: 30017

jwt_secret: keyboardcat
#prefix: banana

ingress:
  enabled: true
  host: neo4jauth.test.home.maximemoreillon.com
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
