{{- if not .Values.mongodb.enabled }}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "resources.prefix" . }}-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "resources.prefix" . }}-mongo
  template:
    metadata:
      labels:
        app: {{ include "resources.prefix" . }}-mongo
    spec:
      containers:
      - name: {{ include "resources.prefix" . }}-mongo
        image: mongo:4
        ports:
        - containerPort: 27017
        volumeMounts:
        - mountPath: /data/db
          name: {{ include "resources.prefix" . }}-mongo
      volumes:
      - name: {{ include "resources.prefix" . }}-mongo
        persistentVolumeClaim:
          claimName: {{ include "resources.prefix" . }}-mongo

{{- end }}
