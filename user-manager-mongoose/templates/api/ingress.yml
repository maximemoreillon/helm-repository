{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "resources.prefix" . }}-api
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - api.{{ include "ingress.host.full" . }}
      secretName: {{ include "resources.prefix" . }}-api
  rules:
    - host: api.{{ include "ingress.host.full" . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service: 
                name: {{ include "resources.prefix" . }}-api
                port:
                  number: 80
{{- end }}
